# AI Agents for Beginners - 学习指南与课程总结

本指南提供了「AI Agents for Beginners」课程的总结，并解释了构建 AI 代理的关键概念、框架和设计模式。

## 1. AI 代理简介

**什么是 AI 代理？**
AI 代理是通过赋予大型语言模型 (LLMs) 访问**工具**、**知识**和**记忆**的能力来扩展其功能的系统。与仅基于训练数据生成文本的标准 LLM 聊天机器人不同，AI 代理可以：
- **感知**其环境（通过传感器或输入）。
- **推理**如何解决问题。
- **行动**来改变环境（通过执行器或工具执行）。

**代理的关键组件：**
- **环境**：代理运行的空间（例如，预订系统）。
- **传感器**：收集信息的机制（例如，读取 API）。
- **执行器**：执行操作的机制（例如，发送电子邮件）。
- **大脑 (LLM)**：规划和决定采取哪些行动的推理引擎。

## 2. 代理框架

本课程涵盖了三种构建代理的主要框架：

| 框架 | 重点 | 最适合 |
|------|------|--------|
| **Semantic Kernel** | 适用于 .NET/Python 的生产级 SDK | 企业应用，将 AI 与现有代码集成。 |
| **AutoGen** | 多代理协作 | 需要多个专业代理相互通信的复杂场景。 |
| **Azure AI Agent Service** | 托管云服务 | 具有内置状态管理的安全、可扩展部署。 |

## 3. 代理设计模式

设计模式有助于构建代理可靠解决问题的结构。

### **工具使用模式**（第 4 课时）
此模式使代理能够与外部世界交互。
- **概念**：为代理提供一个"模式"（可用函数及其参数的列表）。LLM 根据用户的请求决定调用*哪个*工具以及使用*什么*参数。
- **流程**：用户请求 → LLM → **工具选择** → **工具执行** → LLM（带有工具输出）→ 最终响应。
- **用例**：检索实时数据（天气、股票价格）、执行计算、运行代码。

### **规划模式**（第 7 课时）
此模式使代理能够解决复杂的多步骤任务。
- **概念**：代理将高级目标分解为一系列较小的子任务。
- **方法**：
  - **任务分解**：将"计划旅行"拆分为"预订航班"、"预订酒店"、"租车"。
  - **迭代规划**：根据先前步骤的输出重新评估计划（例如，如果航班已满，选择不同的日期）。
- **实现**：通常涉及一个"规划器"代理，该代理生成结构化计划（例如，JSON），然后由其他代理执行。

## 4. 设计原则

设计代理时，考虑三个维度：
- **空间**：代理应连接人员和知识，易于访问但不引人注目。
- **时间**：代理应从*过去*学习，在*现在*提供相关提示，并为*未来*适应。
- **核心**：接受不确定性，但通过透明度和用户控制建立信任。

## 5. 关键课时总结

- **第 1 课时**：代理是系统，而不仅仅是模型。它们感知、推理和行动。
- **第 2 课时**：像 Semantic Kernel 和 AutoGen 这样的框架抽象了工具调用和状态管理的复杂性。
- **第 3 课时**：设计时考虑透明度和用户控制。
- **第 4 课时**：工具是代理的"手"。模式定义对于 LLM 理解如何使用它们至关重要。
- **第 7 课时**：规划是代理的"执行功能"，使其能够处理复杂的工作流程。